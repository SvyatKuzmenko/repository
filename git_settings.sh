#!/bin/sh

  echo "Set git config"
  git config --local core.autocrlf input
  git config --local user.name "Svyat"
  git config --local user.email "SvyatKuzmenko@email.com"
  git config user.name
  git config user.email
